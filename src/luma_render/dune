(library
  (public_name luma.render)
  (libraries 
    luma.app 
    luma.asset 
    luma.driver
    luma.image 
    luma.math 
    ppx_component)
  (modules Render Camera_component Camera_plugin)
  (name luma__render)
  (preprocess 
   (pps ppx_assert ppx_component)))
