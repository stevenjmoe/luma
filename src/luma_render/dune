(library
  (public_name luma.render)
  (libraries luma.asset luma.ecs luma.image luma.math ppx_component)
  (modules Camera Render)
  (name luma__render)
  (preprocess 
   (pps ppx_assert ppx_component)))
