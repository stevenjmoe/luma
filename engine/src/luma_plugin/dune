(include_subdirs qualified)
(library
  (public_name luma.plugin)
  (libraries 
    luma.audio
    luma.codecs
    luma.render 
    luma.time 
    luma.window 
    luma.scene
    luma.debug)
  (name luma__plugin)
  (preprocess 
   (pps ppx_assert)))
